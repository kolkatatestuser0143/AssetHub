import { ConflictException, ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../../common/database/prisma.service';
import { AuthContext } from '../../common/guards/tenant-context.guard';
import { AssetCondition, AssetLifecycleState } from '../../common/enums';
import { EntitlementService } from '../billing/entitlement.service';
import { allowedLifecycleTransitions, assertLifecycleTransition } from './asset-lifecycle';
@Injectable()
export class AssetsService {
  constructor(private readonly prisma:PrismaService,private readonly entitlements:EntitlementService){}
  private async asset(auth:AuthContext,id:string){const a=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.findFirst({where:{id,tenantId:auth.tenantId,...(auth.crossCompany?{}:{companyId:auth.companyId})}}));if(!a)throw new NotFoundException('Asset not found in your scope');return a;}
  async listAssets(auth:AuthContext){return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.findMany({where:{tenantId:auth.tenantId,...(auth.crossCompany?{}:{companyId:auth.companyId})},orderBy:{createdAt:'desc'}}));}
  async listAssetTypes(auth:AuthContext){return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.findMany({where:{companyId:auth.companyId},orderBy:{name:'asc'}}));}
  async createAssetType(auth:AuthContext,name:string,rule:{prefix:string;separator?:string;padding?:number}){const n=name.trim(),p=rule.prefix.trim().toUpperCase();if(!n||!p)throw new ConflictException('Asset type name and prefix are required');const dup=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.findFirst({where:{companyId:auth.companyId,OR:[{name:n},{prefix:p}]}}));if(dup)throw new ConflictException('Another asset type already uses this name or prefix');return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.create({data:{companyId:auth.companyId,name:n,prefix:p,separator:rule.separator??'-',padding:Math.max(1,rule.padding??6),nextSequence:1}}));}
  async deleteAssetType(auth:AuthContext,id:string){const type=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.findFirst({where:{id,companyId:auth.companyId}}));if(!type)throw new NotFoundException('Asset type not found');const count=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.count({where:{assetTypeId:id,companyId:auth.companyId}}));if(count)throw new ConflictException('Asset type is referenced by assets and cannot be deleted');await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.delete({where:{id}}));return {ok:true};}
  async updateCondition(auth:AuthContext,id:string,condition:AssetCondition){if(!Object.values(AssetCondition).includes(condition))throw new ForbiddenException('Invalid asset condition');await this.asset(auth,id);return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.update({where:{id},data:{condition}}));}
  async createAsset(auth:AuthContext,assetTypeId:string,fields:Record<string,unknown>){const type=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetType.findFirst({where:{id:assetTypeId,companyId:auth.companyId}}));if(!type)throw new ForbiddenException('Asset type does not belong to your company');const count=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.count({where:{tenantId:auth.tenantId}}));await this.entitlements.requireWithinLimit(auth.tenantId,'max_assets',count,1);const locationId=typeof fields.locationId==='string'?fields.locationId:undefined,departmentId=typeof fields.departmentId==='string'?fields.departmentId:undefined,vendorId=typeof fields.vendorId==='string'?fields.vendorId:undefined;if(locationId){const location=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.location.findFirst({where:{id:locationId,site:{tenantId:auth.tenantId,companyId:auth.companyId}}}));if(!location)throw new NotFoundException('Location not found');if(departmentId){const d=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.department.findFirst({where:{id:departmentId,locationId}}));if(!d)throw new ForbiddenException('departmentId does not belong to the selected location');}}else if(departmentId){const d=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.department.findFirst({where:{id:departmentId,location:{site:{tenantId:auth.tenantId,companyId:auth.companyId}}}}));if(!d)throw new NotFoundException('Department not found');}if(vendorId){const v=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.findFirst({where:{id:vendorId,companyId:auth.companyId}}));if(!v)throw new ForbiddenException('vendorId does not belong to your company');}const serial=typeof fields.serialNumber==='string'?fields.serialNumber.trim():'';if(serial){const dup=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.findFirst({where:{tenantId:auth.tenantId,serialNumber:serial}}));if(dup)throw new ConflictException(`Serial number '${serial}' is already assigned to asset ${dup.assetNumber}`);}const number=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,async tx=>{const updated=await tx.assetType.update({where:{id:assetTypeId},data:{nextSequence:{increment:1}}});const seq=updated.nextSequence-1;return `${updated.prefix??'AST'}${updated.separator??'-'}${String(seq).padStart(updated.padding??6,'0')}`;});const custom={...fields};for(const k of ['locationId','departmentId','vendorId','condition','serialNumber','model'])delete custom[k];const condition=Object.values(AssetCondition).includes(String(fields.condition) as AssetCondition)?String(fields.condition):AssetCondition.GOOD;try{return await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.create({data:{tenantId:auth.tenantId,companyId:auth.companyId,assetTypeId,assetNumber:number,status:AssetLifecycleState.IN_STOCK,condition,serialNumber:serial||null,model:typeof fields.model==='string'?fields.model.trim()||null:null,locationId:locationId??null,departmentId:departmentId??null,vendorId:vendorId??null,barcodeValue:number,customFields:custom}}));}catch(e:any){if(e?.code==='P2002')throw new ConflictException('Asset could not be created because a unique value is already in use');throw e;}}
  async listAssignments(auth:AuthContext){const rows=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetAssignment.findMany({where:{asset:{tenantId:auth.tenantId,...(auth.crossCompany?{}:{companyId:auth.companyId})}},orderBy:{assignedAt:'desc'},include:{asset:true,user:{select:{id:true,email:true,firstName:true,lastName:true,isActive:true,companyId:true}}}}));return rows.map(a=>({...a,active:!a.returnedAt}));}
  async listAssignmentHistory(auth:AuthContext,assetId:string){await this.asset(auth,assetId);return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetAssignment.findMany({where:{assetId},orderBy:{assignedAt:'desc'},include:{user:{select:{id:true,email:true,firstName:true,lastName:true,companyId:true}}}}));}
  async getCurrentAssignment(auth:AuthContext,assetId:string){await this.asset(auth,assetId);return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetAssignment.findFirst({where:{assetId,returnedAt:null},orderBy:{assignedAt:'desc'},include:{user:{select:{id:true,email:true,firstName:true,lastName:true,companyId:true}}}}));}
  async allowedLifecycleTransitions(auth:AuthContext,id:string){const a=await this.asset(auth,id);return allowedLifecycleTransitions(a.status as AssetLifecycleState);}
  async transitionState(auth:AuthContext,id:string,toState:AssetLifecycleState,actorUserId:string,reason?:string){const a=await this.asset(auth,id);assertLifecycleTransition(a.status as AssetLifecycleState,toState,reason);return this.prisma.withTenantContext(auth.tenantId,auth.companyId,async tx=>{const changed=await tx.asset.updateMany({where:{id,tenantId:auth.tenantId,status:a.status,...(auth.crossCompany?{}:{companyId:auth.companyId})},data:{status:toState}});if(!changed.count)throw new ConflictException('Asset changed before lifecycle transition; retry');await tx.assetAuditEvent.create({data:{tenantId:auth.tenantId,companyId:a.companyId,assetId:id,fromState:a.status,toState,actorUserId,reason:reason?.trim()||null,occurredAt:new Date()}});return tx.asset.findUniqueOrThrow({where:{id}});});}
  async getReportSummary(auth:AuthContext){const assets=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.findMany({where:{tenantId:auth.tenantId,...(auth.crossCompany?{}:{companyId:auth.companyId})}}));const ids=assets.map(a=>a.id);const [assignments,vendors,maintenance,warranties]=await Promise.all([this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetAssignment.findMany({where:{assetId:{in:ids}},select:{assetId:true,userId:true,returnedAt:true}})),this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.findMany({where:{companyId:auth.companyId},select:{id:true}})),this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.assetMaintenance.findMany({where:{tenantId:auth.tenantId,companyId:auth.companyId,assetId:{in:ids}},select:{assetId:true,nextServiceDate:true}})),this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.warranty.findMany({where:{tenantId:auth.tenantId,companyId:auth.companyId,assetId:{in:ids}}}))]);const now=Date.now(),in30=now+30*86400000,statusCounts:Record<string,number>={},conditionCounts:Record<string,number>={};for(const a of assets){statusCounts[a.status]=(statusCounts[a.status]??0)+1;conditionCounts[a.condition]=(conditionCounts[a.condition]??0)+1;}const expired=warranties.filter(w=>w.expiresAt&&w.expiresAt.getTime()<now).length,expiring=warranties.filter(w=>w.expiresAt&&w.expiresAt.getTime()>=now&&w.expiresAt.getTime()<=in30).length,overdue=maintenance.filter(m=>m.nextServiceDate&&m.nextServiceDate.getTime()<now).length,due=maintenance.filter(m=>m.nextServiceDate&&m.nextServiceDate.getTime()>=now&&m.nextServiceDate.getTime()<=in30).length;return {generatedAt:new Date().toISOString(),totals:{assets:assets.length,assignedAssets:assignments.filter(a=>!a.returnedAt).length,assignmentRecords:assignments.length,vendors:vendors.length,warranties:warranties.length,expiredWarranties:expired,expiringWarranties:expiring,overdueMaintenance:overdue,dueMaintenance:due},statusCounts,conditionCounts};}
  async listVendors(auth:AuthContext){return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.findMany({where:{companyId:auth.companyId},orderBy:{name:'asc'}}));}
  async createVendor(auth:AuthContext,name:string,contact?:string){const current=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.count({where:{companyId:auth.companyId}}));await this.entitlements.requireWithinLimit(auth.tenantId,'max_vendors',current,1);try{return await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.create({data:{companyId:auth.companyId,name:name.trim(),contact:contact?.trim()||null}}));}catch(e:any){if(e?.code==='P2002')throw new ConflictException('Vendor already exists');throw e;}}
  async updateVendor(auth:AuthContext,id:string,name:string,contact?:string){const result=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.updateMany({where:{id,companyId:auth.companyId},data:{name:name.trim(),contact:contact?.trim()||null}}));if(!result.count)throw new NotFoundException('Vendor not found');return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.findUniqueOrThrow({where:{id}}));}
  async deleteVendor(auth:AuthContext,id:string){const referenced=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.asset.count({where:{vendorId:id,companyId:auth.companyId}}));if(referenced)throw new ConflictException('Vendor is referenced by an asset');const result=await this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.vendor.deleteMany({where:{id,companyId:auth.companyId}}));if(!result.count)throw new NotFoundException('Vendor not found');return {ok:true};}
  async listWarranties(auth:AuthContext){return this.prisma.withTenantContext(auth.tenantId,auth.companyId,tx=>tx.warranty.findMany({where:{tenantId:auth.tenantId,companyId:auth.companyId},orderBy:{expiresAt:'asc'}}));}
}
