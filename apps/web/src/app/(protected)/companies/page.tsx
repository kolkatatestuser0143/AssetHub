'use client';
import { useEffect,useState } from 'react';
import { apiFetch } from '../../../lib/api-client';
import { Building2, Plus, Search } from 'lucide-react';
export default function CompaniesPage(){const [items,setItems]=useState<any[]>([]);const [name,setName]=useState('');const [code,setCode]=useState('');const [q,setQ]=useState('');const [error,setError]=useState<string|null>(null);const load=async()=>{try{setItems(await apiFetch('/companies'))}catch(e:any){setError(e.message)}};useEffect(()=>{load()},[]);const create=async(e:React.FormEvent)=>{e.preventDefault();try{await apiFetch('/companies',{method:'POST',body:JSON.stringify({name,code:code.toUpperCase()})});setName('');setCode('');load()}catch(e:any){setError(e.message)}};const rows=items.filter(x=>`${x.name} ${x.code}`.toLowerCase().includes(q.toLowerCase()));return <div className="mx-auto max-w-6xl space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-blue-600">Organization</p><h1 className="mt-1 text-2xl font-semibold text-slate-950">Companies & structure</h1><p className="mt-1 text-sm text-slate-500">Manage tenant companies and their operational hierarchy.</p></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><form onSubmit={create} className="grid gap-3 md:grid-cols-[1fr_180px_auto]"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Company name" className="h-10 rounded-xl border border-slate-200 px-3 text-sm"/><input required value={code} onChange={e=>setCode(e.target.value)} placeholder="Code" className="h-10 rounded-xl border border-slate-200 px-3 text-sm uppercase"/><button className="inline-flex h-10 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white"><Plus size={16}/>Add company</button></form></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 p-4"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-2.5 text-slate-400" size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search companies" className="h-9 w-full rounded-lg border border-slate-200 pl-9 text-sm"/></div><span className="ml-4 text-xs text-slate-500">{rows.length} companies</span></div>{error&&<div className="m-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{rows.length===0?<div className="p-12 text-center"><Building2 className="mx-auto text-slate-300" size={34}/><p className="mt-3 font-medium">No companies found</p></div>:<table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Company</th><th className="px-5 py-3">Code</th><th className="px-5 py-3">ID</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(x=><tr key={x.id} className="hover:bg-slate-50"><td className="px-5 py-4 font-medium text-slate-900">{x.name}</td><td className="px-5 py-4"><span className="rounded-lg bg-slate-100 px-2 py-1 font-mono text-xs">{x.code}</span></td><td className="px-5 py-4 font-mono text-xs text-slate-400">{x.id}</td></tr>)}</tbody></table>}</section></div>}
