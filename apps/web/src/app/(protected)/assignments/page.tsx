export default function AssignmentsPage(){return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-slate-400">Operations</p><h1 className="mt-1 text-2xl font-bold text-slate-950">Assignments</h1><p className="mt-2 text-sm text-slate-500">Track asset ownership, transfers, returns, and assignment history.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{['Active','Pending','Returned','History'].map((label)=><div className="panel p-5" key={label}><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-sm font-semibold text-slate-900">API endpoint pending</p></div>)}</div><div className="panel p-8"><p className="font-semibold text-slate-900">Assignment workflow</p><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">The current API exposes asset lifecycle operations but does not yet expose a dedicated assignment list endpoint. This screen is intentionally not populated with fabricated records.</p></div></div>}
