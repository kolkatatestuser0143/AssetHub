'use client';
import { useEffect,useState } from 'react';
import { apiFetch } from '../../../lib/api-client';
export default function IdentityPage(){const [companies,setCompanies]=useState<any[]>([]);const [companyId,setCompanyId]=useState('');const [name,setName]=useState('');const [protocol,setProtocol]=useState<'OIDC'|'SAML'>('OIDC');const [config,setConfig]=useState('{}');const [mapping,setMapping]=useState('{}');const [message,setMessage]=useState('');useEffect(()=>{apiFetch('/companies').then(x=>setCompanies(x)).catch(()=>{})},[]);const save=async(e:React.FormEvent)=>{e.preventDefault();setMessage('');try{await apiFetch(`/identity-providers/${companyId}`,{method:'POST',body:JSON.stringify({name,protocol,config:JSON.parse(config),attributeMapping:JSON.parse(mapping)})});setMessage('Identity provider configuration created.')}catch(e:any){setMessage(e.message)}};return <div className="mx-auto max-w-6xl space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-blue-600">Enterprise identity</p><h1 className="mt-1 text-2xl font-semibold text-slate-950">Identity & SSO</h1><p className="mt-1 text-sm text-slate-500">Configure the identity-provider records exposed by the API.</p></div><section className="max-w-3xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><form onSubmit={save} className="space-y-4"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">Company<select required value={companyId} onChange={e=>setCompanyId(e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"><option value="">Select company</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name} ({c.code})</option>)}</select></label><label className="text-sm font-medium">Provider name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm" placeholder="Corporate SSO"/></label></div><label className="text-sm font-medium">Protocol<select value={protocol} onChange={e=>setProtocol(e.target.value as any)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"><option>OIDC</option><option>SAML</option></select></label><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">Provider config (JSON)<textarea value={config} onChange={e=>setConfig(e.target.value)} rows={8} className="mt-1 w-full rounded-xl border border-slate-200 p-3 font-mono text-xs"/></label><label className="text-sm font-medium">Attribute mapping (JSON)<textarea value={mapping} onChange={e=>setMapping(e.target.value)} rows={8} className="mt-1 w-full rounded-xl border border-slate-200 p-3 font-mono text-xs"/></label></div>{message&&<div className="rounded-xl bg-slate-50 p-3 text-sm text-slate-700">{message}</div>}<button className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">Create provider</button></form></section></div>}
