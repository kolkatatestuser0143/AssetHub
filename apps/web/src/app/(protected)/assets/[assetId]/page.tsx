'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, FileText, History, Package, ShieldCheck, UserRound, AlertTriangle } from 'lucide-react';
import { apiFetch } from '../../../../lib/api-client';
import AssetDocumentsPanel from '../../../../components/assets/AssetDocumentsPanel';
import AssetMaintenancePanel from '../../../../components/assets/AssetMaintenancePanel';
import AssetLabel from '../../../../components/assets/AssetLabel';

const STATES = ['REQUESTED', 'IN_STOCK', 'ASSIGNED', 'IN_REPAIR', 'LOST_STOLEN', 'RETIRED', 'DISPOSED'];
const CONDITIONS = ['NEW', 'GOOD', 'FAIR', 'DAMAGED', 'NEEDS_INSPECTION'];
type Asset = { id: string; assetNumber: string; status: string; condition?: string; serialNumber?: string; model?: string; assetType?: { name: string }; assignment?: { userId?: string; notes?: string; assignedAt?: string; user?: { id: string; employeeId?: string; firstName: string; lastName: string; email: string } | null }; [key: string]: unknown };
type Warranty = { provider?: string; expiresAt?: string } | null;
type TimelineEvent = { type: string; timestamp?: string | null; title: string; description?: string; actorUserId?: string | null };

export default function AssetDetailPage({ params }: { params: { assetId: string } }) {
  const [asset, setAsset] = useState<Asset | null>(null); const [warranty, setWarranty] = useState<Warranty>(null); const [customFields, setCustomFields] = useState<Record<string, unknown>>({}); const [timeline, setTimeline] = useState<TimelineEvent[]>([]); const [state, setState] = useState(''); const [condition, setCondition] = useState('GOOD'); const [provider, setProvider] = useState(''); const [expiresAt, setExpiresAt] = useState(''); const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(true);
  async function load() { setBusy(true); setMessage(null); try { const detail = await apiFetch(`/assets/${params.assetId}`); setAsset(detail); setState(detail.status); setCondition(detail.condition ?? 'GOOD'); const [warrantyData, fieldsData, timelineData] = await Promise.allSettled([apiFetch(`/assets/${params.assetId}/warranty`), apiFetch(`/assets/${params.assetId}/custom-fields`), apiFetch(`/assets/${params.assetId}/timeline`)]); if (warrantyData.status === 'fulfilled') { setWarranty(warrantyData.value ?? null); setProvider(warrantyData.value?.provider ?? ''); setExpiresAt(warrantyData.value?.expiresAt ? String(warrantyData.value.expiresAt).slice(0, 10) : ''); } if (fieldsData.status === 'fulfilled') setCustomFields(fieldsData.value?.values ?? fieldsData.value ?? {}); if (timelineData.status === 'fulfilled') setTimeline(Array.isArray(timelineData.value?.events) ? timelineData.value.events : []); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to load asset.'); } finally { setBusy(false); } }
  useEffect(() => { void load(); }, [params.assetId]);
  async function transition(nextState: string) { if (!asset || nextState === asset.status) return; let reason: string | undefined; if (nextState === 'LOST_STOLEN' || nextState === 'RETIRED' || nextState === 'DISPOSED') { reason = window.prompt(`Reason for ${nextState.replace('_', ' ').toLowerCase()}:`)?.trim(); if (!reason) { setState(asset.status); return; } } try { await apiFetch(`/assets/${asset.id}/transition`, { method: 'POST', body: JSON.stringify({ toState: nextState, reason }) }); setMessage(nextState === 'LOST_STOLEN' ? 'Asset marked lost/stolen. Assignment actions remain blocked until recovery.' : `Asset moved to ${nextState}.`); await load(); } catch (error) { setState(asset.status); setMessage(error instanceof Error ? error.message : 'Unable to change lifecycle state.'); } }
  async function saveCondition(nextCondition: string) { try { await apiFetch(`/assets/${params.assetId}/condition`, { method: 'PATCH', body: JSON.stringify({ condition: nextCondition }) }); setCondition(nextCondition); setMessage('Asset condition updated.'); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to update condition.'); } }
  async function saveWarranty(event: React.FormEvent) { event.preventDefault(); try { await apiFetch(`/assets/${params.assetId}/warranty`, { method: 'PUT', body: JSON.stringify({ provider: provider || undefined, expiresAt: expiresAt ? new Date(`${expiresAt}T00:00:00.000Z`).toISOString() : undefined }) }); setMessage('Warranty saved.'); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to save warranty.'); } }
  const warrantyLabel = useMemo(() => warranty?.expiresAt ? new Date(warranty.expiresAt).toLocaleDateString() : 'Not configured', [warranty]);
  if (busy) return <div className="space-y-4"><div className="h-8 w-48 animate-pulse rounded-lg bg-slate-200"/><div className="h-32 animate-pulse rounded-2xl bg-slate-100"/><div className="h-64 animate-pulse rounded-2xl bg-slate-100"/></div>;
  if (!asset) return <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-sm text-red-700">{message ?? 'Asset not found.'}</div>;
  const assignee = asset.assignment?.user;
  return <div className="mx-auto max-w-[1400px] space-y-6">
    <div className="flex items-center justify-between"><Link href="/assets" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-slate-900"><ArrowLeft size={16}/>Back to assets</Link><div className="flex gap-2"><Link href={`/assets/${params.assetId}/acknowledgement`} className="ui-interactive inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm font-semibold"><FileText size={15}/>Acknowledgement</Link><Link href={`/assets/${params.assetId}/acknowledgement`} className="ui-interactive inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm font-semibold">Label</Link></div></div>
    {message && <div className="rounded-2xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800">{message}</div>}
    {asset.status === 'LOST_STOLEN' && <div className="flex items-start gap-3 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><AlertTriangle size={18} className="mt-0.5 shrink-0"/><div><p className="font-semibold">Asset is marked lost/stolen</p><p className="mt-1">Recovering the asset should move it back to In Stock after inspection.</p></div></div>}
    <header className="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[var(--theme-link)]">Asset 360</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">{asset.assetNumber}</h1><p className="mt-2 text-sm text-slate-500">{asset.assetType?.name ?? 'Unclassified asset'} · {asset.id}</p></div><div className="flex flex-col gap-2 sm:flex-row"><select value={condition} onChange={(event) => { const value = event.target.value; setCondition(value); void saveCondition(value); }} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700" aria-label="Asset condition">{CONDITIONS.map((item) => <option key={item} value={item}>{item.replace('_', ' ')}</option>)}</select><select value={state} onChange={(event) => { const value = event.target.value; setState(value); void transition(value); }} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700" aria-label="Asset lifecycle">{STATES.map((item) => <option key={item} value={item}>{item}</option>)}</select></div></header>
    <div className="grid gap-6 lg:grid-cols-3">
      <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 font-semibold text-slate-900"><Package size={18} className="text-[var(--theme-link)]"/>Overview</div><dl className="mt-5 space-y-4 text-sm"><div className="flex justify-between gap-4"><dt className="text-slate-500">Lifecycle</dt><dd className="font-semibold text-slate-900">{asset.status}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Condition</dt><dd className="font-semibold text-slate-900">{condition.replace('_', ' ')}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Serial</dt><dd className="font-semibold text-slate-900">{asset.serialNumber || '—'}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Warranty</dt><dd className="font-semibold text-slate-900">{warrantyLabel}</dd></div></dl></section>
      <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 font-semibold text-slate-900"><UserRound size={18} className="text-[var(--theme-link)]"/>Current assignee</div><div className="mt-5 rounded-xl bg-slate-50 p-4 text-sm">{assignee ? <><Link href={`/users/${assignee.id}`} className="font-semibold text-slate-900 hover:text-[var(--theme-link)]">{assignee.firstName} {assignee.lastName}</Link><p className="mt-1 text-xs text-slate-500">{assignee.employeeId || assignee.email}</p>{asset.assignment?.assignedAt && <p className="mt-2 text-xs text-slate-500">Assigned {new Date(asset.assignment.assignedAt).toLocaleString()}</p>}</> : <p className="text-slate-500">No active assignment.</p>}</div></section>
      <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 font-semibold text-slate-900"><History size={18} className="text-[var(--theme-link)]"/>Custom fields</div>{Object.keys(customFields).length === 0 ? <p className="mt-5 text-sm text-slate-500">No custom-field values.</p> : <dl className="mt-5 space-y-3 text-sm">{Object.entries(customFields).map(([key, value]) => <div key={key} className="flex justify-between gap-4"><dt className="font-mono text-xs text-slate-500">{key}</dt><dd className="text-right font-medium text-slate-900">{String(value)}</dd></div>)}</dl>}</section>
    </div>
    <AssetLabel assetNumber={asset.assetNumber} assetId={asset.id} />
    <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 font-semibold text-slate-900"><History size={18} className="text-[var(--theme-link)]"/>Asset history</div><p className="mt-1 text-sm text-slate-500">Lifecycle, custody, transfers, documents, maintenance, acknowledgements, and recorded asset activity.</p><div className="mt-6">{timeline.length === 0 ? <div className="rounded-xl bg-slate-50 p-5 text-sm text-slate-500">No history events recorded yet.</div> : timeline.map((event, index) => <div key={`${String(event.timestamp)}-${event.type}-${index}`} className="relative flex gap-4 pb-6 last:pb-0"><div className="relative flex w-4 shrink-0 justify-center"><div className="z-10 mt-1 h-3 w-3 rounded-full bg-[var(--theme-primary)] ring-4 ring-[var(--theme-primary-soft)]"/>{index < timeline.length - 1 && <div className="absolute top-4 h-full w-px bg-slate-200"/>}</div><div className="min-w-0 flex-1 rounded-xl border border-slate-100 bg-slate-50 p-4"><div className="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between"><p className="font-semibold text-slate-900">{event.title}</p><p className="text-xs text-slate-500">{event.timestamp ? new Date(event.timestamp).toLocaleString() : '—'}</p></div><p className="mt-1 text-sm text-slate-600">{event.description ?? '—'}</p>{event.actorUserId && <p className="mt-2 text-xs text-slate-400">Actor: {event.actorUserId}</p>}</div></div>)}</div></section>
    <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 font-semibold text-slate-900"><ShieldCheck size={18} className="text-[var(--theme-link)]"/>Warranty</div><form onSubmit={saveWarranty} className="mt-5 grid gap-4 md:grid-cols-[1fr_220px_auto]"><input value={provider} onChange={(e) => setProvider(e.target.value)} placeholder="Warranty provider" className="field h-10"/><input type="date" value={expiresAt} onChange={(e) => setExpiresAt(e.target.value)} className="field h-10"/><button className="h-10 rounded-xl bg-[var(--theme-primary)] px-4 text-sm font-semibold text-white">Save warranty</button></form></section>
    <AssetMaintenancePanel assetId={params.assetId} /><AssetDocumentsPanel assetId={params.assetId} />
  </div>;
}
