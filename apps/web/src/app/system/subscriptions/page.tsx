'use client';
import { useEffect,useState } from 'react';
import { RefreshCw } from 'lucide-react';
import { systemFetch } from '../../../lib/system-api';
export default function SystemSubscriptionsPage(){const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);try{setItems(await systemFetch('/system/subscriptions'))}catch(e:any){setError(e.message??'Unable to load subscriptions.')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Platform</p><h2 className="mt-1 text-2xl font-bold text-slate-950">Subscriptions & Billing</h2><p className="mt-2 text-sm text-slate-500">Live tenant subscriptions and entitlement state.</p></header>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="flex justify-end"><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700"><RefreshCw className="h-4 w-4"/>Refresh</button></div>{loading?<div className="text-sm text-slate-500">Loading subscriptions…</div>:<div className="grid gap-4 lg:grid-cols-2">{items.map((s:any)=><div key={s.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-950">{s.plan?.name??s.planId??'Unlicensed'}</p><p className="mt-1 text-xs text-slate-500">Tenant: {s.tenantId}</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{s.status}</span></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Started</p><p className="mt-1 font-medium">{s.startedAt?new Date(s.startedAt).toLocaleDateString():'—'}</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Ends</p><p className="mt-1 font-medium">{s.endsAt?new Date(s.endsAt).toLocaleDateString():'No expiry'}</p></div></div></div>)}</div>}</div>}
