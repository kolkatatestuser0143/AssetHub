'use client';
import { useEffect, useState } from 'react';
import { RefreshCw, ShieldCheck } from 'lucide-react';
import { systemFetch } from '../../../lib/system-api';
type Role={id:string;name:string;isSystem:boolean;permissions:{permissionKey?:string}[]};
export default function SystemRolesPage(){const [items,setItems]=useState<Role[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);try{setItems(await systemFetch('/system/roles'))}catch(e:any){setError(e.message??'Unable to load platform roles.')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Platform</p><h2 className="mt-1 text-2xl font-bold text-slate-950">Roles & Permissions</h2><p className="mt-2 text-sm text-slate-500">Platform-level RBAC definitions and privileged permissions.</p></header>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex justify-end border-b border-slate-100 p-4"><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700"><RefreshCw className="h-4 w-4"/>Refresh</button></div>{loading?<div className="p-6 text-sm text-slate-500">Loading roles…</div>:<div className="divide-y divide-slate-100">{items.map(r=><div key={r.id} className="p-5"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-slate-950">{r.name}</p><p className="mt-1 font-mono text-xs text-slate-400">{r.id}</p></div>{r.isSystem&&<span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">System</span>}</div><div className="mt-4 flex flex-wrap gap-2">{r.permissions.map(p=><span key={p.permissionKey} className="inline-flex items-center gap-1.5 rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-700"><ShieldCheck className="h-3 w-3"/>{p.permissionKey}</span>)}</div></div>)}</div>}</section></div>}
