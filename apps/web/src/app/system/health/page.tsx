'use client';
import { useEffect,useState } from 'react';
import { RefreshCw } from 'lucide-react';
import { systemFetch } from '../../../lib/system-api';
export default function SystemHealthPage(){const [data,setData]=useState<any>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);setError('');try{setData(await systemFetch('/system/health'))}catch(e:any){setError(e.message??'Unable to load health.')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Operations</p><h2 className="mt-1 text-2xl font-bold text-slate-950">System Health</h2><p className="mt-2 text-sm text-slate-500">Live platform health signals.</p></header>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="flex justify-end"><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700"><RefreshCw className="h-4 w-4"/>Refresh</button></div>{loading?<div className="text-sm text-slate-500">Checking services…</div>:<><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{data.status}</span><p className="mt-3 text-xs text-slate-500">Checked {new Date(data.checkedAt).toLocaleString()}</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Object.entries(data.checks).map(([name,check]:any)=><div key={name} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h3 className="font-semibold capitalize text-slate-950">{name}</h3><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{check.status}</span></div><p className="mt-3 text-sm text-slate-500">{check.detail??'Platform health signal'}</p></div>)}</div></>}</div>}
