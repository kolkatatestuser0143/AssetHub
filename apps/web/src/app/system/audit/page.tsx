const filters = ['Actor', 'Tenant', 'Action', 'Resource', 'Severity', 'Date range'];

export default function SystemAuditPage() {
  return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Security</p><h2 className="mt-1 text-2xl font-bold text-slate-950">Audit & security</h2><p className="mt-2 text-sm text-slate-500">Review privileged platform activity, authentication events and security signals.</p></div><div className="flex flex-wrap gap-2">{filters.map((f) => <button key={f} className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">{f}</button>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><h3 className="font-semibold text-slate-950">Audit stream</h3><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">The current API does not yet expose a platform-wide audit query endpoint. The screen is intentionally empty until that endpoint exists.</p><div className="mt-5 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><p className="text-sm font-semibold text-slate-700">No platform audit data available</p><p className="mt-1 text-xs text-slate-500">No mock events are shown.</p></div></div></div>;
}
