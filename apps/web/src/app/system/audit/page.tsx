'use client';
import { useEffect, useState } from 'react';
import { RefreshCw } from 'lucide-react';
import { systemFetch } from '../../../lib/system-api';
type Event={id:string;tenantId?:string|null;actorUserId?:string|null;action:string;resourceType:string;resourceId?:string|null;result?:string;method?:string|null;route?:string|null;statusCode?:number|null;occurredAt:string};
export default function SystemAuditPage(){const [items,setItems]=useState<Event[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);try{setItems(await systemFetch('/system/audit'))}catch(e:any){setError(e.message??'Unable to load audit data.')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Security</p><h2 className="mt-1 text-2xl font-bold text-slate-950">Audit & Security</h2><p className="mt-2 text-sm text-slate-500">Review platform-wide authenticated activity.</p></header>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex justify-end border-b border-slate-100 p-4"><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700"><RefreshCw className="h-4 w-4"/>Refresh</button></div>{loading?<div className="p-6 text-sm text-slate-500">Loading audit events…</div>:items.length===0?<div className="p-12 text-center text-sm text-slate-500">No audit events recorded yet.</div>:<div className="divide-y divide-slate-100">{items.map(e=><div key={e.id} className="grid gap-3 p-5 md:grid-cols-[1fr_1fr_180px]"><div><p className="font-semibold text-slate-950">{e.action}</p><p className="mt-1 text-xs text-slate-500">{e.resourceType}{e.resourceId?` · ${e.resourceId}`:''}</p></div><div className="text-xs text-slate-500"><p>Actor: {e.actorUserId??'system'}</p><p className="mt-1">Tenant: {e.tenantId??'platform'}</p></div><div className="text-xs text-slate-500"><p>{e.method} {e.route}</p><p className="mt-1">{e.statusCode??''} · {new Date(e.occurredAt).toLocaleString()}</p></div></div>)}</div>}</section></div>}
