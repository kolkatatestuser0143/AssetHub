'use client';
import { useEffect,useState } from 'react';
import Link from 'next/link';
import { systemFetch } from '../../lib/system-api';
export default function SystemDashboard(){const [data,setData]=useState<any>(null);const [error,setError]=useState('');useEffect(()=>{systemFetch('/system/overview').then(setData).catch((e:any)=>setError(e.message??'Unable to load overview.'))},[]);return <div className="space-y-7"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Platform</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Platform overview</h1><p className="mt-2 text-sm text-slate-500">Live tenant, user, asset and subscription metrics.</p></header>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>} {!data?<div className="text-sm text-slate-500">Loading overview…</div>:<><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Tenants',data.tenants,'/system/tenants'],['Tenant users',data.users,'/system/users'],['Assets',data.assets,'/system/analytics'],['Subscriptions',data.subscriptions,'/system/subscriptions']].map(([a,b,href]:any)=><Link href={href} key={a} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-blue-200 hover:shadow-md"><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold text-slate-950">{b}</p><p className="mt-3 text-xs font-semibold text-blue-600">Open workspace →</p></Link>)}</div><div className="grid gap-4 md:grid-cols-3"><Link href="/system/health" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-semibold">System health</p><p className="mt-1 text-sm text-slate-500">Live service checks and infrastructure signals.</p></Link><Link href="/system/audit" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-semibold">Audit & security</p><p className="mt-1 text-sm text-slate-500">Review platform-wide authenticated activity.</p></Link><Link href="/system/analytics" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-semibold">Platform analytics</p><p className="mt-1 text-sm text-slate-500">Explore aggregated tenant and asset metrics.</p></Link></div></>}</div>}
